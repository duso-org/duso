// Example using include() to share helpers

include("colors.du")
include("helpers.du")

// Now we can use functions from helpers.du
print(greet("Alice"))
print(greet("Bob"))

print("Sum:", add(5, 3))

items = ["apple", "banana", "cherry"]
print("Items:", format_list(items))
