// Regular assignment with constant - no compound fast path
// This pattern: sum = sum + var + const should be slower than +=

sum = 0
start = timer()

for i = 1, 100000 do
  sum = sum + i + 5
end

elapsed = (timer() - start) * 1000

print("Regular assign (=): " + sum + " in " + elapsed + "ms")
