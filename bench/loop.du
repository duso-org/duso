// Nested loops - iteration performance
start = now(true)

sum = 0
for i = 1, 1000 do
  for j = 1, 1000 do
    sum += i * j
  end
end

elapsed = now(true) - start

print("Loop sum (1000x1000) = " + sum + " in " + elapsed + "ms")
